## Settings file was created by plugin darmuhsTerminalStuff v3.2.3
## Plugin GUID: darmuh.TerminalStuff

[AlwaysOn]

## Setting this to true will set <alwayson> to enabled at launch.
# Setting type: Boolean
# Default value: true
alwaysOnAtStart = true

## Setting this to true will disable the terminal screen whenever you are not on the ship when alwayson is enabled.
# Setting type: Boolean
# Default value: true
alwaysOnDynamic = true

## Set this to true if you wish to keep the screen on after death.
# Setting type: Boolean
# Default value: false
alwaysOnWhileDead = false

[BioScan]

## Credits cost to upgrade Bioscan command to provide detailed information on scanned enemies.
# Setting type: Int32
# Default value: 300
bioScanUpgradeCost = 300

## Credits cost to run Bioscan command each time it's run.
# Setting type: Int32
# Default value: 15
enemyScanCost = 15

[Cams]

## Message returned when enabling Cams command (cams).
# Setting type: String
# Default value: (CAMS)
camString = (CAMS)

## Message returned when disabling Cams command (cams).
# Setting type: String
# Default value: Cameras disabled.
camString2 = Cameras disabled.

## Message returned when enabling map command (map).
# Setting type: String
# Default value: (MAP)
mapString = (MAP)

## Message returned when disabling map command (map).
# Setting type: String
# Default value: Map View disabled.
mapString2 = Map View disabled.

## Message returned when enabling Overlay command (overlay).
# Setting type: String
# Default value: (Overlay)
ovString = (Overlay)

## Message returned when disabling Overlay command (overlay).
# Setting type: String
# Default value: Overlay disabled.
ovString2 = Overlay disabled.

## Message returned when enabling minimap command (minimap).
# Setting type: String
# Default value: (MiniMap)
mmString = (MiniMap)

## Message returned when disabling minimap command (minimap).
# Setting type: String
# Default value: MiniMap disabled.
mmString2 = MiniMap disabled.

## Message returned when enabling minicams command (minicams).
# Setting type: String
# Default value: (MiniCams)
mcString = (MiniCams)

## Message returned when disabling minicams command (minicams).
# Setting type: String
# Default value: MiniCams disabled.
mcString2 = MiniCams disabled.

## Set the resolution of the Mirror Camera created with OpenBodyCams for darmuhsTerminalStuff
# Setting type: String
# Default value: 1000; 700
obcResolutionMirror = 1000; 700

## Set the resolution of the Body Camera created with OpenBodyCams for darmuhsTerminalStuff
# Setting type: String
# Default value: 1000; 700
obcResolutionBodyCam = 1000; 700

## Setting this to true will make it so no command will ever auto-hide any cams command.
# Setting type: Boolean
# Default value: false
camsNeverHide = true

## Set the default view switch commands will use when nothing is active.
# Setting type: String
# Default value: cams
# Acceptable values: map, cams, minimap, minicams, overlay
defaultCamsView = map

## Opacity percentage for Overlay View.
# Setting type: Int32
# Default value: 10
# Acceptable value range: From 0 to 100
ovOpacity = 10

[Comfort Commands (On/Off)]

## Shows the current lobby name <Lobby Name>
# Setting type: Boolean
# Default value: true
terminalLobby = true

## Command to quit terminal <Quit>
# Setting type: Boolean
# Default value: true
terminalQuit = true

## Command to clear terminal text <Clear>
# Setting type: Boolean
# Default value: true
terminalClear = false

## Command to heal yourself <Heal>
# Setting type: Boolean
# Default value: true
terminalHeal = false

## Command to change your FOV <Fov>
# Setting type: Boolean
# Default value: true
terminalFov = false

## Command to see your active mods <Mods>
# Setting type: Boolean
# Default value: true
terminalMods = true

## Enables kick command for host. <Kick>
# Setting type: Boolean
# Default value: false
terminalKick = false

## Command to toggle Always-On Display <Alwayson>
# Setting type: Boolean
# Default value: true
terminalAlwaysOn = true

## Use [purchasePackCommands] to create purchase packs that contain multiple store items in one run of the command
# Setting type: Boolean
# Default value: true
terminalPurchasePacks = false

[Controls Commands (On/Off)]

## Pull the lever from terminal <Lever>
# Setting type: Boolean
# Default value: true
terminalLever = false

## Check moon danger level <Danger>
# Setting type: Boolean
# Default value: true
terminalDanger = false

## Command to Activate Teleporter <TP>
# Setting type: Boolean
# Default value: true
terminalTP = false

## Command to Activate Inverse Teleporter <ITP>
# Setting type: Boolean
# Default value: true
terminalITP = false

## Command to open/close the ship door. <Door>
# Setting type: Boolean
# Default value: true
terminalDoor = false

## Command to toggle the ship lights
# Setting type: Boolean
# Default value: true
terminalLights = false

## Command to toggle the terminal Clock off/on <Clock>
# Setting type: Boolean
# Default value: true
terminalClockCommand = false

## Command to restart the lobby (skips firing sequence) <Restart>
# Setting type: Boolean
# Default value: true
terminalRestart = false

[Custom Keywords]

## This semi-colon separated list is all keywords that can be used in terminal to return <alwayson> command
# Setting type: String
# Default value: alwayson;always on
alwaysOnKeywords = alwayson;always on

## This semi-colon separated list is all keywords that can be used in terminal to return <cams> command
# Setting type: String
# Default value: cameras; show cams; cams
camsKeywords = cameras; show cams; cams

## Additional This semi-colon separated list is all keywords that can be used in terminal to return <map> command
# Setting type: String
# Default value: show map; map; view monitor
mapKeywords = show map; map; view monitor

## This semi-colon separated list is all keywords that can be used in terminal to return <minimap> command.
# Setting type: String
# Default value: minimap; show minimap
minimapKeywords = minimap; show minimap

## This semi-colon separated list is all keywords that can be used in terminal to return <minicams> command
# Setting type: String
# Default value: minicams; show minicams
minicamsKeywords = minicams; show minicams

## This semi-colon separated list is all keywords that can be used in terminal to return <overlay> command
# Setting type: String
# Default value: overlay; show overlay
overlayKeywords = overlay; show overlay

## This semi-colon separated list is all keywords that can be used in terminal to return <cams> command
# Setting type: String
# Default value: mirror; reflection; show mirror
mirrorKeywords = mirror; reflection; show mirror

## This semi-colon separated list is all keywords that can be used in terminal to return <door> command
# Setting type: String
# Default value: door; toggle door
doorKeywords = door; toggle door

## This semi-colon separated list is all keywords that can be used in terminal to return <lights> command
# Setting type: String
# Default value: lights; toggle lights
lightsKeywords = lights; toggle lights

## This semi-colon separated list is all keywords that can be used in terminal to return <mods> command
# Setting type: String
# Default value: modlist; mods; show mods
modsKeywords = modlist; mods; show mods

## This semi-colon separated list is all keywords that can be used in terminal to return <tp> command
# Setting type: String
# Default value: tp; use teleporter; teleport
tpKeywords = tp; use teleporter; teleport

## This semi-colon separated list is all keywords that can be used in terminal to return <itp> command
# Setting type: String
# Default value: itp; use inverse; inverse
itpKeywords = itp; use inverse; inverse

## This semi-colon separated list is all keywords that can be used in terminal to return <quit> command
# Setting type: String
# Default value: quit;exit;leave
quitKeywords = quit;exit;leave

## This semi-colon separated list is all keywords that can be used in terminal to return <video> command
# Setting type: String
# Default value: lol; play video; lolxd; hahaha
videoKeywords = lol; play video; lolxd; hahaha

## This semi-colon separated list is all keywords that can be used in terminal to return <clear> command
# Setting type: String
# Default value: clear;wipe;clean
clearKeywords = clear;wipe;clean

## This semi-colon separated list is all keywords that can be used in terminal to return <danger> command
# Setting type: String
# Default value: danger;hazard;show danger; show hazard
dangerKeywords = danger;hazard;show danger; show hazard

## This semi-colon separated list is all keywords that can be used in terminal to return <heal> command
# Setting type: String
# Default value: healme; heal me; heal; medic
healKeywords = healme; heal me; heal; medic

## This semi-colon separated list is all keywords that can be used in terminal to return <loot> command
# Setting type: String
# Default value: loot; shiploot; show loot
lootKeywords = loot; shiploot; show loot

## This semi-colon separated list is all keywords that can be used in terminal to return <randomsuit> command
# Setting type: String
# Default value: randomsuit; random suit
randomSuitKeywords = randomsuit; random suit

## This semi-colon separated list is all keywords that can be used in terminal to toggle Terminal Clock display
# Setting type: String
# Default value: clock; show clock; time; show time
clockKeywords = clock; show clock; time; show time

## This semi-colon separated list is all keywords that can be used in terminal to return <itemlist> command
# Setting type: String
# Default value: show items; get items; listitem
ListItemsKeywords = show items; get items; listitem

## This semi-colon separated list is all keywords that can be used in terminal to return <lootlist> command
# Setting type: String
# Default value: loot detail; listloost
ListScrapKeywords = loot detail; listloot; listscraps

## This semi-colon separated list is all keywords that can be used in terminal to return <lootlist> command
# Setting type: String
# Default value: route random; random moon
randomRouteKeywords = route random; random moon

## This semi-colon separated list is all keywords that can be used in terminal to return <lobby> command
# Setting type: String
# Default value: lobby; show lobby; lobby name; show lobby name
lobbyKeywords = lobby; show lobby; lobby name; show lobby name

## Set the keyword that can be used in terminal to return <fcolor> command
# Setting type: String
# Default value: fcolor
fcolorKeyword = fcolor

## Set the keyword that that can be used in terminal to return <gamble> command
# Setting type: String
# Default value: gamble
gambleKeyword = gamble

## This semi-colon separated list is all keywords that can be used in terminal to return <lever> command
# Setting type: String
# Default value: lever
leverKeywords = lever

## Set the keyword that that can be used in terminal to return <scolor> command
# Setting type: String
# Default value: scolor
scolorKeyword = scolor

## Set the keyword that that can be used in terminal to return <link> command
# Setting type: String
# Default value: link
linkKeyword = link

## Set the keyword that that can be used in terminal to return <link2> command
# Setting type: String
# Default value: link2
link2Keyword = link2

[Debug]

## Enable or Disable extensive logging for this mod.
# Setting type: Boolean
# Default value: false
extensiveLogging = false

## Enable or Disable developer logging for this mod. (this will fill your log file FAST)
# Setting type: Boolean
# Default value: false
developerLogging = false

[Door]

## Message returned on door (open) command.
# Setting type: String
# Default value: Opening door.
doorOpenString = Opening door.

## Message returned on door (close) command.
# Setting type: String
# Default value: Closing door.
doorCloseString = Closing door.

## Message returned on door (inSpace) command.
# Setting type: String
# Default value: Can\'t open doors in space.
doorSpaceString = Can\'t open doors in space.

## Set this to true to allow for pressing the button to open the door in space. (does not change whether the door can actually be opened)
# Setting type: Boolean
# Default value: false
canOpenDoorInSpace = false

[Extras Commands (On/Off)]

## Command to show total onboard loot value <Loot>
# Setting type: Boolean
# Default value: true
terminalLoot = false

## Command to toggle displaying cameras in terminal <Cameras>
# Setting type: Boolean
# Default value: true
terminalCams = false

## Scan player being tracked by monitor for their Health/Weight. <Vitals>
# Setting type: Boolean
# Default value: true
terminalVitals = false

## Scan for "non-employee" lifeforms. <BioScan>
# Setting type: Boolean
# Default value: true
terminalBioScan = false

## Purchase-able upgrade patch to bioscan for more precise information. <BioScan>
# Setting type: Boolean
# Default value: true
terminalBioScanPatch = false

## Purchase-able upgrade to vitals to not cost anything each scan. <Vitals>
# Setting type: Boolean
# Default value: true
terminalVitalsUpgrade = false

## Adds 'map' shortcut to 'view monitor' command <Map>
# Setting type: Boolean
# Default value: true
terminalMap = true

## Command to view cams with radar at the top right. <Minimap>
# Setting type: Boolean
# Default value: true
terminalMinimap = false

## Command to view radar with cams at the top right. <Minicams>
# Setting type: Boolean
# Default value: true
terminalMinicams = false

## Command to view cams with radar overlayed on top. <Overlay>
# Setting type: Boolean
# Default value: true
terminalOverlay = false

## Command to link to an external web-page <Link>
# Setting type: Boolean
# Default value: true
terminalLink = false

## Command to link to a second external web-page <Link2>
# Setting type: Boolean
# Default value: false
terminalLink2 = false

## Command to list all non-scrap & not currently held items on the ship <ItemsList>
# Setting type: Boolean
# Default value: true
terminalListItems = true

## Command to display an extensive list of all scrap on the ship <LootList>
# Setting type: Boolean
# Default value: true
terminalLootDetail = false

## Command to toggle displaying a Mirror Cam in the terminal <Mirror>
# Setting type: Boolean
# Default value: true
terminalMirror = false

## Command to cancel an undelivered order and get your credits back <Refund>
# Setting type: Boolean
# Default value: true
terminalRefund = false

## Command to switch back to previous radar target <Previous>
# Setting type: Boolean
# Default value: true
terminalPrevious = false

[Fun Commands (On/Off)]

## Play a video from the videoFolderPath folder <video>
# Setting type: Boolean
# Default value: true
terminalVideo = false

## Command to gamble your credits, by percentage <Gamble>
# Setting type: Boolean
# Default value: true
terminalGamble = false

## Command to change flashlight color. <Fcolor colorname>
# Setting type: Boolean
# Default value: true
terminalFcolor = false

## Command to change ship lights colors. <Scolor all,front,middle,back colorname>
# Setting type: Boolean
# Default value: true
terminalScolor = false

## Command to switch your suit from a random one off the rack <RandomSuit>
# Setting type: Boolean
# Default value: true
terminalRandomSuit = true

## Command to route to a random planet, configurable <Previous>
# Setting type: Boolean
# Default value: true
terminalRouteRandom = false

[Gamble]

## Credits needed to start gambling, 0 means you can gamble everything.
# Setting type: Int32
# Default value: 0
gambleMinimum = 0

## Enable Gamble Pity Mode, which gives credits back to those who lose everything.
# Setting type: Boolean
# Default value: false
gamblePityMode = false

## If Gamble Pity Mode is enabled, specify how much Pity Credits are given to losers. (Max: 60)
# Setting type: Int32
# Default value: 10
gamblePityCredits = 10

## Message returned when your credits is less than the gambleMinimum set.
# Setting type: String
# Default value: You don\'t meet the minimum credits requirement to gamble.
gamblePoorString = You don\'t meet the minimum credits requirement to gamble.

[General]

## Disable this if you want to disable networking and use this mod as a Client-sided mod
# Setting type: Boolean
# Default value: true
ModNetworking = true

## Enable or Disable the terminalClock
# Setting type: Boolean
# Default value: true
terminalClock = false

## Enable this for the ability to bind commands to any valid key (also enables the "bind" keyword.
# Setting type: Boolean
# Default value: true
terminalShortcuts = true

## With this enabled, this mod will detect if another mod that adds player cams is enabled and use the mod's camera for all cams commands. Currently detects the following: Helmet Cameras by Rick Arg, Body Cameras by Solo, OpenBodyCams by 
# Setting type: Boolean
# Default value: true
camsUseDetectedMods = true

[Heal]

## Message returned when heal command is run and player is already full health.
# Setting type: String
# Default value: You are full health!
healIsFullString = You are full health!

## Message returned when heal command is run and player is healed.
# Setting type: String
# Default value: The terminal healed you?!?
healString = The terminal healed you?!?

[Home Page]

## First line of the home command (startup screen)
# Setting type: String
# Default value: Welcome to the FORTUNE-9 OS PLUS
homeline1 = Welcome to the FORTUNE-9 OS PLUS

## Second line of the home command (startup screen)
# Setting type: String
# Default value: \tUpgraded by Employee: <color=#e6b800>darmuh</color>
homeline2 = \tUpgraded by Employee: <color=#e6b800>darmuh</color>\r\n\t...and compiled by <color=#338ef4>d3st1ny</color>...

## Last line of the home command (startup screen)
# Setting type: String
# Default value: Have a wonderful [currentDay]!
homeline3 = Have a wonderful [currentDay]!

## these two lines should generally be used to point to menus of other usable commands. Can also be expanded to more than two lines by using "\r\n" to indicate a new line
# Setting type: String
# Default value: >>Type \"Help\" for a list of commands.\r\n>>Type <color=#b300b3>\"More\"</color> for a menu of darmuh\'s commands.\r\n
homeHelpLines = >>Type \"Help\" for a list of commands.\r\n

## ASCII Art goes here
# Setting type: String
# Default value: [leadingSpacex4][leadingSpace]<color=#e6b800>^^      .-=-=-=-.  ^^\r\n ^^        (`-=-=-=-=-`)         ^^\r\n         (`-=-=-=-=-=-=-`)  ^^         ^^\r\n   ^^   (`-=-=-=-=-=-=-=-`)   ^^          \r\n       ( `-=-=-=-(@)-=-=-` )      ^^\r\n       (`-=-=-=-=-=-=-=-=-`)  ^^          \r\n       (`-=-=-=-=-=-=-=-=-`)  ^^\r\n        (`-=-=-=-=-=-=-=-`)          ^^\r\n         (`-=-=-=-=-=-=-`)  ^^            \r\n           (`-=-=-=-=-`)\r\n            `-=-=-=-=-`</color>
homeTextArt = [leadingSpacex4][leadingSpace]<color=#e6b800>^^      .-=-=-=-.  ^^</color>\r\n <color=#e6b800>^^        (`-=-=-=-=-`)         ^^</color>\r\n <color=#e6b800>        (`-=-=-=-=-=-=-`)  ^^                 ^^</color>\r\n <color=#e6b800>  ^^   (`-=-=-=-=-=-=-=-`)   ^^ </color>\r\n <color=#e6b800>      ( `-=-=- </color><color=#e5e5e5>%@% </color><color=#e6b800>-=-=</color><color=#e5e5e5> #@%</color><color=#e6b800>     </color>\r\n <color=#e6b800>      (`-=-=-= </color><color=#e5e5e5>%@% </color><color=#e6b800>=-=-</color><color=#e5e5e5> *#+</color><color=#e6b800>  ^^ </color>\r\n <color=#e6b800>      (`-=-=-= </color><color=#e5e5e5>%@# </color><color=#e6b800>=-` </color><color=#e5e5e5> `  </color><color=#e6b800>                 ^^   </color>\r\n <color=#e6b800>       (`-=-=- </color><color=#e5e5e5>`          .</color><color=#e6b800>              (@)</color>\r\n <color=#e6b800>        (`-=-= </color><color=#e5e5e5>   . </color><color=#e6b800>-=-</color><color=#e5e5e5> #@#</color><color=#e6b800> ^      ^^</color>\r\n <color=#e6b800>          (`-=.</color><color=#e5e5e5> #%#-..-#@@:</color>\r\n <color=#e6b800>           `-=-</color><color=#e5e5e5> `+%@@@@%+´ </color><color=#e6b800>   ^^</color>  <color=#666666>undefin</color><color=#e5e5e5>3</color><color=#666666>d</color>

[Lever]

## Message returned on lever pull command.
# Setting type: String
# Default value: PULLING THE LEVER!!!
leverString = PULLING THE LEVER!!!

## Setting this to true will disable the confirmation check for the <lever> command.
# Setting type: Boolean
# Default value: false
leverConfirmOverride = false

[Link]

## URL to send players to when using the "link" command.
# Setting type: String
# Default value: https://thunderstore.io/c/lethal-company/p/darmuh/darmuhsTerminalStuff/
customLink = https://thunderstore.io/c/lethal-company/p/darmuh/darmuhsTerminalStuff/

## Hint given to players in extras menu for "link" command.
# Setting type: String
# Default value: Go to a specific web page.
customLinkHint = Go to a specific web page.

## URL to send players to when using the second "link" command.
# Setting type: String
# Default value: https://github.com/darmuh/TerminalStuff
customLink2 = https://github.com/darmuh/TerminalStuff

## Hint given to players in extras menu for "link" command.
# Setting type: String
# Default value: Go to a specific web page.
customLink2Hint = Go to a specific web page.

[Networked Things]

## Enable networked Always-On Display & displaying synced terminal nodes (BETA)
# Setting type: Boolean
# Default value: false
networkedNodes = true

[Purchase packs]

## List of purchase pack commands to create. Format is command:item1,item2,etc.;next command:item1,item2
# Setting type: String
# Default value: Essentials:pro,shov,walkie;PortalPack:teleporter,inverse
purchasePackCommands = Essentials:pro,shov,walkie;PortalPack:teleporter,inverse

[Quality of Life]

## Enable or Disable the ability to use a walkie from your inventory at the terminal (vanilla method still works)
# Setting type: Boolean
# Default value: true
walkieTerm = true

## Key used to activate your walkie while at the terminal, see here for valid key names https://docs.unity3d.com/Packages/com.unity.inputsystem@1.0/api/UnityEngine.InputSystem.Key.html
# Setting type: String
# Default value: LeftAlt
walkieTermKey = LeftAlt

## Mousebutton used to activate your walkie while at the terminal, see here for valid button names https://docs.unity3d.com/Packages/com.unity.inputsystem@1.3/api/UnityEngine.InputSystem.LowLevel.MouseButton.html
# Setting type: String
# Default value: Left
walkieTermMB = Left

## Enable this to lock the player camera to the terminal when it is in use.
# Setting type: Boolean
# Default value: false
LockCameraInTerminal = false

## Enable this config item to disable the light that turns on when you start using the terminal.
# Setting type: Boolean
# Default value: false
DisableTerminalLight = false

## (Requires terminalShortcuts feature to function) With this feature enabled, uparrow and downarrow will cycle through a list of previously used commands.
# Setting type: Boolean
# Default value: false
TerminalHistory = true

## Max amount of previous commands to save in TerminalHistory list.
# Setting type: Int32
# Default value: 9
# Acceptable value range: From 3 to 50
TerminalHistoryMaxCount = 9

## (Requires terminalShortcuts feature to function) With this feature enabled, tab key will cycle through a list of matching commands to the current input.
# Setting type: Boolean
# Default value: false
TerminalAutoComplete = false

## Key used to activate TerminalAutoComplete feature https://docs.unity3d.com/Packages/com.unity.inputsystem@1.0/api/UnityEngine.InputSystem.Key.html
# Setting type: String
# Default value: Tab
TerminalAutoCompleteKey = Tab

## Max amount of matching commands to store before disabling autocomplete.
# Setting type: Int32
# Default value: 5
# Acceptable value range: From 3 to 50
TerminalAutoCompleteMaxCount = 5

[Quit]

## Message returned on quit command.
# Setting type: String
# Default value: goodbye!
quitString = goodbye!

[Restart]

## Setting this to true will disable the confirmation check for the <restart> command.
# Setting type: Boolean
# Default value: false
restartConfirmOverride = false

[Route Random]

## This semi-colon separated list is all keywords that can be used in terminal to return <alwayson> command
# Setting type: String
# Default value: Eclipsed;Flooded;Foggy
routeRandomBannedWeather = Eclipsed;Flooded;Foggy

## Flat rate for running the route random command to get a random moon...
# Setting type: Int32
# Default value: 100
# Acceptable value range: From 0 to 99999
routeRandomCost = 100

[Teleporters]

## Message returned when TP command is run.
# Setting type: String
# Default value: Teleport Button pressed.
tpMessageString = Teleport Button pressed.

## Message returned when ITP command is run.
# Setting type: String
# Default value: Inverse Teleport Button pressed.
itpMessageString = Inverse Teleport Button pressed.

[Terminal Customization]

## Enable or Disable this section (all terminal customizations)
# Setting type: Boolean
# Default value: false
TerminalCustomization = false

## This changes the color of the physical terminal
# Setting type: String
# Default value: #666633
TerminalColor = #666633

## This changes the color of the main text in the terminal
# Setting type: String
# Default value: #ffffb3
TerminalTextColor = #ffffb3

## This changes the color of the current credits text in the top left of the terminal
# Setting type: String
# Default value: #ccffcc
TerminalMoneyColor = #ccffcc

## This changes the color of the text caret in the terminal
# Setting type: String
# Default value: #9900ff
TerminalCaretColor = #9900ff

[Terminal Shortcuts]

## Stored keybinds, don't modify this unless you know what you're doing!
# Setting type: String
# Default value: 
keyActionsConfig = 

[Video]

## Message displayed when first playing a video.
# Setting type: String
# Default value: lol.
videoStartString = lol.

## Message displayed if you want to end video playback early.
# Setting type: String
# Default value: No more lol.
videoStopString = No more lol.

## Folder name where videos will be pulled from, needs to be in BepInEx/plugins
# Setting type: String
# Default value: darmuh-darmuhsTerminalStuff
videoFolderPath = darmuh-darmuhsTerminalStuff

## When networking is enabled, this setting will sync videos being played on the terminal for all players whose terminal screen is on.
# Setting type: Boolean
# Default value: true
videoSync = true

[Vitals]

## Message returned when you don't have enough credits to run the <Vitals> command.
# Setting type: String
# Default value: You can\'t afford to run this command.
vitalsPoorString = You can\'t afford to run this command.

## Message returned when you don't have enough credits to unlock the vitals scanner upgrade.
# Setting type: String
# Default value: You can\'t afford to upgrade the Vitals Scanner.
vitalsUpgradePoor = You can\'t afford to upgrade the Vitals Scanner.

## Credits cost to run Vitals Command each time it's run.
# Setting type: Int32
# Default value: 10
vitalsCost = 10

## Credits cost to upgrade Vitals command to not cost credits anymore.
# Setting type: Int32
# Default value: 200
vitalsUpgradeCost = 200

